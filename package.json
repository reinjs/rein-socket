{
  "name": "@reinjs/rein-socket",
  "version": "1.0.2",
  "description": "socket plugin of reinjs",
  "author": "reinjs",
  "license": "MIT",
  "dependencies": {
    "@reinjs/rein-class": "^1.0.2",
    "socket.io": "^2.1.1"
  },
  "devDependencies": {
    "@reinjs/rein-cli": "^1.0.6"
  },
  "plugin": {
    "name": "socket"
  },
  "scripts": {
    "postinstall": "./node_modules/.bin/rc init @reinjs/rein-socket",
    "dev": "NODE_ENV=dev node index.js --service.name=socket-plugin --service.tag=dev --service.port=8080"
  }
}
